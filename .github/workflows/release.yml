name: Release
on: {workflow_dispatch: {}, workflow_call: {}, push: {tags: ['v*', 'data-*']}}
permissions: {contents: read}
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7 # v4
      - run: make release-check
