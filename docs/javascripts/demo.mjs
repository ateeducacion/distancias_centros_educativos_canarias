const form=document.querySelector("#route-demo");if(form){const params=new URLSearchParams(location.search);origin.value=params.get("origin")??"";destination.value=params.get("destination")??"";form.addEventListener("submit",event=>{event.preventDefault();result.textContent="La publicación de datos cargará aquí la matriz estática coherente.";history.replaceState(null,"",`?origin=${origin.value}&destination=${destination.value}`)})}
