# Fuentes

El recurso `centros.csv` se resuelve con `package_show` de CKAN verificando nombre, formato y estado. OpenStreetMap Canarias se obtiene de Geofabrik. Se registran URL final, ETag, Last-Modified, tamaño y SHA-256.
