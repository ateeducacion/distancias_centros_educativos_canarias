# Overrides auditables

Están desactivados por defecto. Cada JSON debe declarar `center_code`, `field`, `old_value`, `new_value`, `reason`, `source`, `created_at` y `expires_at`. Nunca se usan para ocultar un problema de la fuente.
